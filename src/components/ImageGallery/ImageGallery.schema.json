{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-site.dev/schemas/components/ImageGallery.schema.json",
  "title": "ImageGallery",
  "type": "object",
  "additionalProperties": false,
  "required": ["variant", "content"],
  "properties": {
    "variant": { "type": "string", "enum": ["grid", "masonry", "lightbox"] },
    "content": {
      "type": "object",
      "additionalProperties": false,
      "required": ["items"],
      "properties": {
        "heading": { "type": "string" },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["alt"],
            "properties": {
              "alt": { "type": "string", "minLength": 1 },
              "caption": { "type": "string" }
            }
          }
        }
      }
    },
    "settings": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "maxWidth": { "type": "string", "enum": ["narrow", "wide", "full"] }
      }
    },
    "className": { "type": "string" },
    "contentPathPrefix": { "type": "string" }
  }
}
